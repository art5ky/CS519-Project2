--- linux-5.15.0-orig/kernel/exit.c	2026-04-06 13:03:23.000000000 -0600
+++ linux-5.15.0/kernel/exit.c	2026-04-12 17:07:06.026543170 -0600
@@ -65,6 +65,7 @@
 #include <linux/compat.h>
 #include <linux/io_uring.h>
 #include <linux/sysfs.h>
+#include <linux/extent.h>
 
 #include <linux/uaccess.h>
 #include <asm/unistd.h>
@@ -869,6 +870,16 @@
 
 	exit_mm();
 
+	// Patched section
+	/*
+	After exiting, print the stats of the extent table then free it
+	 */
+	if (current->et) {
+        extent_table_print_stats(current->et, current->pid);
+        extent_table_free(current->et);
+        current->et = NULL;
+    }
+
 	if (group_dead)
 		acct_process();
 	trace_sched_process_exit(tsk);
